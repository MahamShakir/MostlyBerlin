# TICKET-I124 — AI Review of Kafka Configuration

## Prompt Used

Review this Spring Kafka configuration for production readiness. Focus on: ack mode, retry strategy, consumer-group rebalance, and DLT handling.

---

## Files Reviewed

- `application.yml`
- `KafkaConfig.java`

---

## Findings

| Category | Finding | Decision |
|---|---|---|
| already done | Producer uses `acks=all` for stronger message durability. | Accepted. Already implemented. |
| already done | DefaultErrorHandler with DeadLetterPublishingRecoverer provides retry and DLT handling. | Accepted. Already implemented. |
| already done | ErrorHandlingDeserializer prevents poison messages from blocking partitions permanently. | Accepted. Already implemented. |
| out of scope | Consumer group rebalance tuning could be improved with additional production settings such as cooperative assignment strategies. | Deferred. Current application scale does not require this. |
| legit + actionable | Producer idempotence was not explicitly enabled. Kafka defaults can allow duplicate records during retries. | Fixed by enabling `enable.idempotence=true`. |

---

## Accepted Fix

Finding:

Producer idempotence was not explicitly enabled.

Fix:

Added:

```yaml
spring:
  kafka:
    producer:
      properties:
        enable.idempotence: true